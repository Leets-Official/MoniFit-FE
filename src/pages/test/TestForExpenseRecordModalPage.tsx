// import { Button, ExpenseRecordModal } from "@/components";
// import { useState } from "react";

export const TestForExpenseRecordModalPage = () => {
  // const [showModal, setShowModal] = useState(false);

  return (
    <>
      <main className="relative flex h-screen w-full flex-col items-center justify-start gap-4 bg-black pt-10 opacity-70">
        {/* <Button onClick={() => setShowModal(true)}>모달 열기</Button>
        {showModal && (
          <ExpenseRecordModal onClose={() => setShowModal(false)} onSave={function (expense: number, category: string): void {
            throw new Error("Function not implemented.");
          } } />
        )} */}
      </main>
    </>
  );
};
